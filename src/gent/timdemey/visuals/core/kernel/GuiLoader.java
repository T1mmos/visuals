package gent.timdemey.visuals.core.kernel;

/**
 * Represents a {@link Loader} that attaches parts to a {@link DefaultGuiBuilder} (or
 * a subclass thereof).
 * @author Timmos
 */
public interface GuiLoader extends Loader<DefaultGuiBuilder> {
    // no extra methods
}
