package gent.timdemey.visuals.core.ui;

import gent.timdemey.visuals.core.key.Key;

/**
 * Identifies a renderable entity, so tells "what" is being rendered to a {@link RendererFactory}.
 * @author Timmos
 * @see RenderFlavor
 */
public interface RenderKey extends Key {
    // marker interface
}
