package core.ui;

/**
 * Hints on how a renderable entity (identified by a {@link RenderKey}) should be rendered. For example, a person's
 * nationality (renderable entity 'Nationality') could be rendered in text flavor ("Belgian") or visual flavor
 * (using a country flag). Plugins may add custom flavors.
 * @author Timmos
 * @see RenderKey
 */
public interface RenderFlavor {
    // marker interface
}
