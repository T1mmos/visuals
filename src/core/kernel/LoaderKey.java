package core.kernel;

import core.key.Key;

/**
 * Marker interface for keys with the purpose of identifying {@link Loader}s.
 * @author Timmos
 */
public interface LoaderKey extends Key {
    // marker interface
}
