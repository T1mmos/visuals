package core.kernel;

/**
 * Represents a {@link Loader} that attaches parts to a {@link DefaultAppBuilder} (or
 * a subclass thereof).
 * @author Timmos
 */
public interface AppLoader extends Loader<DefaultAppBuilder> {
    // no extra methods
}
