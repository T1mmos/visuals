package core.cfg;

/**
 * Enumerates the possible types of a configuration key.
 * @author Timmos
 */
public enum CfgKeyType {
	/** Type used for meta configuration keys. */
	Meta,
	/** Type used for standard configuration keys. */
	Normal,
	/** Type used for plugin configuration keys. */
	Plugin
}
